<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.franklin.mapper.DeptMapper">

    <select id="getAll" resultType="com.franklin.dto.DeptDTO">
        select * from dept order by id asc
    </select>
    <select id="get" resultType="com.franklin.dto.DeptDTO">
        select id, name, create_time, update_time from dept where id = #{id}
    </select>

    <delete id="delete">
        delete from dept where id = #{id}
    </delete>

    <insert id="create">
        insert into dept(name, create_time, update_time) values(#{name}, NOW(), NOW())
    </insert>

    <update id="update">
        update dept set name = #{name}, update_time = NOW() where id = #{id}
    </update>

</mapper>